import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff'
    },
    teacherList:{
        marginTop: '-4%',
        padding: 16,
    }
})

export default styles